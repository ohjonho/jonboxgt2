
// main.js — central loader to glue all modules

document.addEventListener('DOMContentLoaded', () => {
  console.log('Main game loaded. Modules initialized.');

  // Example boot flow:
  // fetchPlayers, update UI, start game when host clicks
  // additional coordination between modules can go here
});
